from Student import Student
from StudentList import StudentList

def run_StudentList_tests():  
    pass

"""
    test_add()  
    test_deleteByGroup()

def test_deleteByGroup():
    sList = StudentList()
    sList.add(2, "Andrei2", 2)
    sList.add(3, "Andre3", 2)
    sList.add(19, "Andrei4", 3)
    sList.add(5, "Andrei5", 2)

    sList.deleteByGroup(2)

    if sList.getData() != [[19, 'Andrei4', 3]]:
        raise Exception("deleteByGroup not working")

def test_add():
    ssList = StudentList()
    ssList.add(2, "Andrei4", 3)
    
    try:
        ssList.add(4, "Andrei5", 2)
       # assert(False)
    except Exception as ex:
        assert(str(ex) == "id taken")

"""